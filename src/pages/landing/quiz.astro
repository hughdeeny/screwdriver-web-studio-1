---
import LandingLayout from "../../layouts/LandingLayout.astro";
import QuizPage from "../../components/landing/QuizPage";

// Native quiz form redirects to /landing/thank-you on submit (see QuizForm.tsx).
// If you replace this with a GoHighLevel or other embedded form, set that form's
// post-submit redirect to /landing/thank-you in the external provider's settings
// so the Meta Pixel Lead event still fires.
---

<LandingLayout
  title="Free Reputation Health Check | Screwdriver Marketing"
  description="Answer 15 quick questions and get your free Reputation Health Score with immediate recommendations."
>
  <QuizPage client:load />
</LandingLayout>
